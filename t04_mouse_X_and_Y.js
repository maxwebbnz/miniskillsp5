/*
function setup(){
  createCanvas(710, 400);
  background(102);
  }

function draw(){
  stroke(240);
  if (mouseIsPressed === true) {
    line(mouseX, mouseY, pmouseX, pmouseY);
  }

  // communicate to the user where the mouse is currently
  text("Your Mouse is Currently at: X = " + mouseX + " Y = " + mouseY, width/2, height-40)
}
*/