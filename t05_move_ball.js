/*
var ballx = 40
var bally = 40


var moveAtRandom = random();

function setup(){
createCanvas(400,400)
}
function draw(){
  background(0,100,255)
  text("Press Mouse to Move ball to you!", width/2, height-20)
  createBall()
}

// ballhandler

function createBall(){
  ellipse(ballx, bally, 40,40)
  if(mouseIsPressed){
    ballx = mouseX
    bally = mouseY
  }
}
*/