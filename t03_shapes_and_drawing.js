/*
function setup() {
  createCanvas(400, 400);
}

function draw() {
  // changing background colour to blue for task
  // you cant use background(blue) for this**
  background(0, 0, 255);
  // drawing a rectangle, at 20 x, 20y, with a width and height of 200
  rect(20,20, 200,200)
  fill(0,0,259)
  stroke(0,255,0)

  // drawing a rectangle at 20 x but at 50y, with the same size as the rectangle
  line(20,50, 200)
  // drawing a ellipse at the centre of the screen, 20 pxs above the bottom of the screen, with a height and width of 20 
  ellipse(width/2, height-20, 20,20)
  // second rectangle

  rect(width/2, height-30,200,200)


}
*/